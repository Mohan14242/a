- name: testing the variables
  hosts: localhost
  vars_prompt:
    - name: name
      prompt: please enter your name 


  tasks:
  - name: displaying the multiple variables
    ansible.builtin.debug:
      msg: "your sting is converted into the upper case{{name | split(' ')}}"

 