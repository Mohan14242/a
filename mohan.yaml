- name: testing the variables
  hosts: localhost
  vars:
    list1:
      - 1
      - 2
      - 3
      - 4
      - 5
      - 6
    list2: 
      - 7 
      - 8
      - 9
    combined_list:[]
  tasks:
  - name: combning the list
    ansible.builtin.set_fact:
      nothing: "{{combined_list + list1}}"
  - name: cobinnt the list2
    ansible.builtin.debug:
      msg: "{{nothing+list2}}"


   